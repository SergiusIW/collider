Libraries need to be placed in a "libs/" directory in order for the 
project to compile correctly.

Collider-demos is dependent on the Collider jar file (download link will 
be added shortly).

Collider-demos is dependent on a LibGDX jar file (see 
http://libgdx.badlogicgames.com/download.html).

Binaries to be placed in libs/:
 * collider.jar
 * gdx.jar
 
Source code to be placed in libs/:
 * collider-sources.jar
 * gdx-sources.jar
